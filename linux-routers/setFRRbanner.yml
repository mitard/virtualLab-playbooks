- name: Initialisation de la bannière post-connexion sur un routeur FRR
  hosts: "{{ target }}"
  become: yes

  tasks:
    - name: Initialisation du fichier /etc/frr/banner
      ansible.builtin.shell:
        cmd: figlet -f standard -c {{ bannerTxt }} > /etc/frr/banner
